# Production Environment Configuration Example
# Copy this to .env.production and fill in your values

# Core Configuration (REQUIRED)
TELEGRAM_TOKEN=your_production_bot_token_here
ENCRYPTION_KEY=generate_with_openssl_rand_-base64_32
MONERO_RPC_URL=http://monero-wallet:18082

# Admin Configuration (REQUIRED)
ADMIN_IDS=123456789,987654321  # Comma-separated list of vendor admin IDs
SUPER_ADMIN_IDS=123456789      # Platform admin who can add vendors
TOTP_SECRET=generate_with_pyotp.random_base32()

# Data Management
DATA_RETENTION_DAYS=30         # How long to keep order data
DEFAULT_COMMISSION_RATE=0.05   # 5% default commission

# Logging Configuration
LOG_LEVEL=INFO                 # Use DEBUG for troubleshooting
LOG_FILE=/app/logs/bot.log     # Log file path inside container

# Production Settings
ENVIRONMENT=production
DATABASE_URL=sqlite:////app/data/db.sqlite3  # Persistent volume path
MAX_RETRIES=3                  # Payment check retries
RETRY_DELAY=5                  # Seconds between retries

# Health Check Configuration
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_PORT=8080         # Port for health endpoints